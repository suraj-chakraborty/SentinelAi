# ── Core Voice Pipeline ─────────────────────────────────────────────────────
pvporcupine==3.0.2
pyaudio>=0.2.14
numpy>=1.26.0
soundfile>=0.12.1
SpeechRecognition>=3.10.1

# ── Neural TTS (Tier 1 upgrade: replaces pyttsx3 as primary) ─────────────────
edge-tts>=6.1.9            # Microsoft neural voices — free, online
pyttsx3>=2.90              # Fallback offline TTS (Windows SAPI)
pygame>=2.5.2              # Audio playback for edge-tts output

# ── Neural STT (Tier 1 upgrade: replaces Google STT as primary) ──────────────
faster-whisper>=1.0.3      # Local Whisper inference (CTranslate2, int8)

# ── AI / LLM ─────────────────────────────────────────────────────────────────
openai>=1.30.0             # OpenRouter + OpenAI compatible SDK
httpx>=0.27.0              # Gemini REST API client
ollama>=0.2.0              # Local LLM via Ollama daemon

# ── RAG / Knowledge Base ─────────────────────────────────────────────────────
chromadb>=0.5.0            # Vector database for semantic search
sentence-transformers>=3.0.0  # Embedding model (all-MiniLM-L6-v2)
networkx>=3.2              # Optional memory graph in KnowledgeBaseModule

# ── OCR / Screen Memory ───────────────────────────────────────────────────────
pytesseract>=0.3.10        # Tesseract OCR wrapper

# ── Vision ────────────────────────────────────────────────────────────────────
opencv-python>=4.10.0      # Camera/webcam capture
mediapipe>=0.10.14         # Hand gesture detection
Pillow>=10.3.0             # Image processing (screenshots)

# ── Web Automation ────────────────────────────────────────────────────────────
playwright>=1.44.0         # Browser automation (primary)
selenium>=4.21.0           # Browser automation (secondary)
webdriver-manager>=4.0.1   # Auto-manage ChromeDriver

# ── Desktop Automation ────────────────────────────────────────────────────────
pyautogui>=0.9.54          # GUI automation
pygetwindow>=0.0.9         # Window management
pywin32>=306               # Windows API access

# ── System Monitoring ─────────────────────────────────────────────────────────
psutil>=5.9.8              # CPU/RAM/disk/network metrics
GPUtil>=1.4.0              # GPU metrics

# ── Security & Cryptography ───────────────────────────────────────────────────
cryptography>=42.0.7       # AES-GCM secrets vault
scapy>=2.5.0               # Network packet analysis for overwatch

# ── IoT ───────────────────────────────────────────────────────────────────────
paho-mqtt>=2.1.0           # MQTT broker communication
requests>=2.32.3           # Home Assistant REST API

# ── Calendar ─────────────────────────────────────────────────────────────────
google-api-python-client>=2.130.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# ── File Intelligence ─────────────────────────────────────────────────────────
pypdf>=4.2.0               # PDF text extraction
python-docx>=1.1.2         # DOCX text extraction

# ── Audio Analysis ────────────────────────────────────────────────────────────
librosa>=0.10.2            # Spectral features for emotion analysis

# ── GUI & Tray ────────────────────────────────────────────────────────────────
pystray>=0.19.5            # System tray icon
fastapi>=0.111.0           # Web dashboard API server
uvicorn[standard]>=0.30.0  # ASGI server
jinja2>=3.1.4              # HTML templates

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1       # .env file loading
pyinstaller>=6.8.0         # App packaging
win10toast>=0.9            # Windows notifications (legacy)
schedule>=1.2.0            # Task scheduling for daemons
docker>=7.0.0              # Docker SDK for sandbox execution

# ── Biometrics / Face Recognition ────────────────────────────────────────────
deepface>=0.0.87           # Face recognition for zero-touch auth
mediapipe>=0.10.14         # Pose detection

# ── Mobile Bridge ─────────────────────────────────────────────────────────────
flask>=3.0.0               # Lightweight server for phone bridge
requests>=2.32.3           # HTTP client for tunnel API

# ── Meeting Surrogate ────────────────────────────────────────────────────────
sounddevice>=0.4.6         # Audio routing for virtual microphone
openai-whisper>=20231117   # Meeting transcription

# ── Voice / Audio ────────────────────────────────────────────────────────────
webrtcvad>=2.0.10          # WebRTC Voice Activity Detection
silero-vad>=1.3.0          # Silero VAD for speech detection

# ── Distributed Computing / Hive ──────────────────────────────────────────────
grpcio>=1.60.0             # gRPC for hive computing
redis>=5.0.0               # Redis for distributed state
ray>=2.8.0                 # Distributed computing framework

# ── Training / Fine-Tuning ────────────────────────────────────────────────────
torch>=2.0.0               # PyTorch for LoRA training
transformers>=4.35.0       # HuggingFace transformers
peft>=0.7.0                # Parameter-efficient fine-tuning
unsloth>=2024.1.0          # Fast LoRA training

# ── AR / Spatial Interface ────────────────────────────────────────────────────
aiohttp>=3.9.0             # Async HTTP for WebXR server
websockets>=12.0           # WebSocket for AR communication

# ── Neuro / Telekinesis ────────────────────────────────────────────────────────
brainflow>=4.8.0           # Brainwave interface SDK
scipy>=1.11.0              # Signal processing for EEG
